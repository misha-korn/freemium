# Core framework
Django>=5.1,<5.3
djangorestframework>=3.15,<3.16

# Auth
django-allauth>=65.0

# Config & DB
django-environ>=0.11
dj-database-url>=2.2
psycopg[binary]>=3.2

# Background tasks & cache (wired in Stage 2, declared now for a stable base)
celery>=5.4
redis>=5.0

# External market-data HTTP calls (provider abstraction)
requests>=2.32

# Static files serving
whitenoise>=6.7

# Excel + PDF export (Stage 5 — Pro tax report / data export)
openpyxl>=3.1
reportlab>=4.0

# i18n catalog tooling without GNU gettext (bin/build_translations.py)
polib>=1.2
